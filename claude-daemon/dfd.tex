\documentclass[a4paper,landscape,11pt,xelatex,ja=standard]{bxjsarticle}
\usepackage{xeCJK}
\setCJKmainfont{Hiragino Mincho ProN}
\setCJKsansfont{Hiragino Kaku Gothic ProN}
\geometry{margin=15mm}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}

\pagestyle{empty}

\begin{document}

\begin{center}
{\Large\bfseries claude-daemon データフロー図}
\end{center}

\vspace{0.3em}

\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    % --- ノードスタイル ---
    process/.style={
        rectangle, rounded corners=6pt, draw=black!80, line width=1.2pt,
        fill=blue!12, minimum width=4.2cm, minimum height=2cm,
        align=center, font=\bfseries
    },
    datastore/.style={
        rectangle, draw=black!60, line width=0.8pt, fill=yellow!12,
        minimum width=3.2cm, minimum height=1cm,
        align=center, font=\small
    },
    external/.style={
        rectangle, draw=black!80, line width=1.4pt, fill=green!8,
        minimum width=3.2cm, minimum height=1cm,
        align=center, font=\small\bfseries
    },
    gate/.style={
        rectangle, rounded corners=3pt, draw=red!60!black, line width=1pt,
        fill=red!8, minimum height=0.8cm, minimum width=2.2cm,
        align=center, font=\scriptsize\bfseries, text=red!70!black,
    },
    % --- 矢印スタイル ---
    flow/.style={-{Stealth[length=7pt,width=5pt]}, line width=1pt, black!70},
    data/.style={-{Stealth[length=6pt,width=4pt]}, line width=0.8pt, blue!50!black, densely dashed},
    % --- ラベルスタイル ---
    lbl/.style={font=\scriptsize, text=black!60, align=center, fill=white, inner sep=1.5pt},
]

% =============================================
%  Daemon オーケストレーター (全体を統括)
% =============================================
\node[process, fill=purple!12, minimum width=5cm] (daemon) at (11, 8)
    {Daemon\\[2pt]{\small\normalfont RunCycle()}\\[1pt]{\scriptsize\normalfont launchd 30分間隔}};

% =============================================
%  メインフロー: プロセスの間にゲートを挟む
%
%  OBSERVE ─→ JUDGE ─→ [Backoff] ─→ [Cooldown] ─→ EXECUTE
%                                                      │
%                                                  [Sandbox]
%                                                      │
%                                                  dotfiles
% =============================================

% --- プロセス ---
\node[process] (observe) at (0, 0)
    {1. OBSERVE\\[2pt]{\small\normalfont observer}\\[1pt]{\scriptsize\normalfont コスト: \$0}};

\node[process] (judge) at (11, 0)
    {2. JUDGE\\[2pt]{\small\normalfont judge}\\[1pt]{\scriptsize\normalfont コスト: \raisebox{0pt}{\~{}}\$0.01--0.50}};

\node[process] (execute) at (22, 0)
    {3. EXECUTE\\[2pt]{\small\normalfont executor}\\[1pt]{\scriptsize\normalfont コスト: \raisebox{0pt}{\~{}}\$1}};

% --- ゲート: フロー矢印上に配置 ---
\node[gate] (g_backoff) at (16.5, 0.5)
    {Backoff\\{\tiny 3連続失敗 → 指数待機}};

\node[gate] (g_cooldown) at (16.5, -0.5)
    {Cooldown\\{\tiny 同一target → 3h制限}};

% --- Daemon → 各プロセスへの制御フロー ---
% Daemon が各フェーズを順に起動し、ゲート判定も Daemon が行う
\draw[flow, purple!60] (daemon.south -| 0, 0) -- (observe.north)
    node[lbl, midway, left=3pt] {(1) 起動};
\draw[flow, purple!60] (daemon.south) -- (judge.north -| 11, 4)
    node[lbl, pos=0.7, left=3pt] {(2) 起動};
\draw[flow, purple!60] (daemon.south -| 22, 0) -- (execute.north -| 22, 4)
    node[lbl, pos=0.7, right=3pt] {(4) 起動};

% Daemon → ゲート判定
\draw[flow, purple!60] (daemon.south -| 16.5, 0) -- (g_backoff.north)
    node[lbl, midway, right=3pt] {(3) 判定};
\draw[flow, purple!60] ([yshift=-2pt] g_backoff.south) -- ([yshift=2pt] g_cooldown.north);

% --- メインフロー矢印 (データの流れ) ---
\draw[data] (observe) -- (judge)
    node[lbl, midway, below=3pt] {統計レポート};

\draw[data] (judge) -- (g_backoff.west);
\draw[data] (judge) -- (g_cooldown.west);
\draw[data] (g_backoff.east) -- (execute)
    node[lbl, midway, above=3pt] {Decision JSON};
\draw[data] (g_cooldown.east) -- (execute);

% --- Sandbox: EXECUTE → dotfiles の間 ---
\node[gate] (g_sandbox) at (22, -3.5)
    {Sandbox\\{\tiny 許可: Read/Edit/Write/Bash(git:*)}\\{\tiny 禁止: claude-daemon/ 改変, ネットワーク}};

\draw[flow] (execute) -- (g_sandbox);
\draw[flow] (g_sandbox) -- ++(0, -2)
    node[lbl, midway, right=2pt] {Edit / Write\\git commit};

% =============================================
%  入力データストア (上段左)
% =============================================
\node[datastore] (usage)    at (-6, 4) {usage/*.tsv\\{\scriptsize スキル使用ログ}};
\node[datastore] (sessions) at (-6, 2.5)  {projects/*.jsonl\\{\scriptsize セッションログ}};
\node[datastore] (errlogs)  at (-6, 1)  {daemon/logs/*.log\\{\scriptsize エラーログ}};

\draw[data] (usage)    -- (observe);
\draw[data] (sessions) -- (observe);
\draw[data] (errlogs)  -- (observe);

% =============================================
%  外部 LLM (上段右)
% =============================================
\node[external] (haiku)  at (11, 4) {Claude Haiku\\{\scriptsize\normalfont ツール使用禁止}};
\node[external] (sonnet) at (22, 4) {Claude Sonnet\\{\scriptsize\normalfont ツール制限付き}};

\draw[flow] ([xshift=2pt] judge.north) -- ([xshift=2pt] haiku.south)
    node[lbl, midway, right=3pt] {プロンプト};
\draw[flow] ([xshift=-2pt] haiku.south) -- ([xshift=-2pt] judge.north)
    node[lbl, midway, left=3pt] {JSON応答};

\draw[flow] ([xshift=2pt] execute.north) -- ([xshift=2pt] sonnet.south)
    node[lbl, midway, right=3pt] {改善指示};
\draw[flow] ([xshift=-2pt] sonnet.south) -- ([xshift=-2pt] execute.north)
    node[lbl, midway, left=3pt] {コード変更};

% =============================================
%  状態データストア (下段)
% =============================================
\node[datastore] (state)    at (-1, -5.5) {daemon/state/\\{\scriptsize observer checkpoint}};
\node[datastore] (history)  at (7, -5.5)  {history.jsonl\\{\scriptsize 改善履歴}};
\node[datastore] (budgetdb) at (14, -5.5) {budget.json\\{\scriptsize 支出記録（上限なし）}};
\node[datastore] (dotfiles) at (22, -7.5) {dotfiles リポジトリ\\{\scriptsize skills/ settings.json fish/ nvim/ git/}};

\draw[data] (observe) -- (state)
    node[lbl, midway, left=2pt] {checkpoint\\読み書き};
\draw[data] (history) -- (judge)
    node[lbl, midway, left=2pt] {直近履歴};
\draw[data] ([xshift=-5pt] execute.south west) |- (history)
    node[lbl, pos=0.25, left=2pt] {結果記録};
\draw[data] ([xshift=5pt] execute.south west) |- (budgetdb)
    node[lbl, pos=0.25, right=2pt] {コスト記録};

% =============================================
%  人間 (右端)
% =============================================
\node[external] (human) at (28, -7.5) {人間};
\draw[flow] (dotfiles) -- (human)
    node[lbl, midway, above=2pt] {git diff / レビュー後 push};

% =============================================
%  凡例 (左下)
% =============================================
\node[anchor=north west] at (-5, -7.5) {
    \begin{tikzpicture}[x=1cm, y=0.55cm]
        \node[font=\small\bfseries, anchor=west] at (0, 0) {凡例:};

        \draw[flow, purple!60] (1.8, 0) -- (3.3, 0);
        \node[font=\scriptsize, anchor=west] at (3.5, 0) {Daemon制御};

        \draw[data] (6, 0) -- (7.5, 0);
        \node[font=\scriptsize, anchor=west] at (7.7, 0) {データフロー};

        \draw[flow] (11, 0) -- (12.5, 0);
        \node[font=\scriptsize, anchor=west] at (12.7, 0) {LLM呼び出し};

        \node[process, minimum width=1.2cm, minimum height=0.5cm, font=\tiny]
            at (1.3, -1) {Process};
        \node[datastore, minimum width=1.2cm, minimum height=0.5cm, font=\tiny]
            at (4, -1) {Data Store};
        \node[external, minimum width=1.2cm, minimum height=0.5cm, font=\tiny]
            at (6.7, -1) {External};
        \node[gate, minimum width=1.2cm, font=\tiny]
            at (9.5, -1) {Gate};
        \node[font=\scriptsize, anchor=west] at (10.5, -1) {フロー上の安全チェック};
    \end{tikzpicture}
};

\end{tikzpicture}%
}% end resizebox

\end{document}
